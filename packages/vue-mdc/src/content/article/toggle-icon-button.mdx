---
headline: 'ToggleIconButton'
description: 'Icon buttons help people take minor actions with one tap.'
collection: 'component'
---
import { ToggleIconButton } from '@glare-labs/vue-mdc/icon-button'
import { Icon } from '@glare-labs/vue-mdc/icon'

# ToggleIconButton

## Required

ToggleIconButton組件需要引入`icon-button`, `ripple`和`elevation`組件的樣式:

```css
@import url('@glare-labs/vue-mdc/build/components/icon-button/styles/icon-button.module.css');
@import url('@glare-labs/vue-mdc/build/components/ripple/styles/ripple.module.css');
@import url('@glare-labs/vue-mdc/build/components/elevation/styles/elevation.module.css');
```

ToggleIconButton組件需要引入`icon-button`組件的核心:

```js
import { ToggleIconButton } from '@glare-labs/vue-mdc/icon-button'
```

若要在Fab中使用圖標, 需要引入Icon組件樣式和核心:

```css
@import url('@glare-labs/vue-mdc/build/components/icon/styles/icon.module.css');
```

```js
import { Icon } from '@glare-labs/vue-mdc/icon'
```

## Props

### Appearance

|Value|EIconButtonAppearance|
|:--|:--|
|`"filled"`|`EIconButtonAppearance.Filled`|
|`"filled-tonal"`|`EIconButtonAppearance.FilledTonal`|
|`"outlined"`|`EIconButtonAppearance.Outlined`|
|`"standard"`|`EIconButtonAppearance.Standard`|

<ToggleIconButton client:load appearance="filled">
    <Icon>send</Icon>
</ToggleIconButton>
<ToggleIconButton client:load appearance="filled-tonal">
    <Icon>send</Icon>
</ToggleIconButton>
<ToggleIconButton client:load appearance="outlined">
    <Icon>send</Icon>
</ToggleIconButton>
<ToggleIconButton client:load appearance="standard">
    <Icon>send</Icon>
</ToggleIconButton>

```html
<ToggleIconButton appearance="filled">
    <Icon>send</Icon>
</ToggleIconButton>
<ToggleIconButton appearance="filled-tonal">
    <Icon>send</Icon>
</ToggleIconButton>
<ToggleIconButton appearance="outlined">
    <Icon>send</Icon>
</ToggleIconButton>
<ToggleIconButton appearance="standard">
    <Icon>send</Icon>
</ToggleIconButton>
```

### DefaultSelected

<ToggleIconButton client:load defaultSelected appearance="filled">
    <Icon>done</Icon>
</ToggleIconButton>
<ToggleIconButton client:load defaultSelected appearance="filled-tonal">
    <Icon>done</Icon>
</ToggleIconButton>
<ToggleIconButton client:load defaultSelected appearance="outlined">
    <Icon>done</Icon>
</ToggleIconButton>
<ToggleIconButton client:load defaultSelected appearance="standard">
    <Icon>done</Icon>
</ToggleIconButton>

### Disabled

<ToggleIconButton client:load disabled appearance="filled">
    <Icon>send</Icon>
</ToggleIconButton>
<ToggleIconButton client:load disabled appearance="filled-tonal">
    <Icon>send</Icon>
</ToggleIconButton>
<ToggleIconButton client:load disabled appearance="outlined">
    <Icon>send</Icon>
</ToggleIconButton>
<ToggleIconButton client:load disabled appearance="standard">
    <Icon>send</Icon>
</ToggleIconButton>

<ToggleIconButton client:load disabled defaultSelected appearance="filled">
    <Icon>done</Icon>
</ToggleIconButton>
<ToggleIconButton client:load disabled defaultSelected appearance="filled-tonal">
    <Icon>done</Icon>
</ToggleIconButton>
<ToggleIconButton client:load disabled defaultSelected appearance="outlined">
    <Icon>done</Icon>
</ToggleIconButton>
<ToggleIconButton client:load disabled defaultSelected appearance="standard">
    <Icon>done</Icon>
</ToggleIconButton>

```html
<ToggleIconButton disabled appearance="filled">
    <Icon>send</Icon>
</ToggleIconButton>
<ToggleIconButton disabled appearance="filled-tonal">
    <Icon>send</Icon>
</ToggleIconButton>
<ToggleIconButton disabled appearance="outlined">
    <Icon>send</Icon>
</ToggleIconButton>
<ToggleIconButton disabled appearance="standard">
    <Icon>send</Icon>
</ToggleIconButton>

<ToggleIconButton disabled defaultSelected appearance="filled">
    <Icon>done</Icon>
</ToggleIconButton>
<ToggleIconButton disabled defaultSelected appearance="filled-tonal">
    <Icon>done</Icon>
</ToggleIconButton>
<ToggleIconButton disabled defaultSelected appearance="outlined">
    <Icon>done</Icon>
</ToggleIconButton>
<ToggleIconButton disabled defaultSelected appearance="standard">
    <Icon>done</Icon>
</ToggleIconButton>
```import { Icon } from '@glare-labs/vue-mdc/icon'
import { ToggleIconButton } from '@glare-labs/vue-mdc/icon-button'
import { Icon } from '@glare-labs/vue-mdc/icon'
import { ToggleIconButton } from '@glare-labs/vue-mdc/icon-button'import { Icon } from '@glare-labs/vue-mdc/icon'
import { ToggleIconButton } from '@glare-labs/vue-mdc/icon-button'
import { Icon } from '@glare-labs/vue-mdc/icon'
import { ToggleIconButton } from '@glare-labs/vue-mdc/icon-button'import { Icon } from '@glare-labs/vue-mdc/icon'
import { ToggleIconButton } from '@glare-labs/vue-mdc/icon-button'
import { Icon } from '@glare-labs/vue-mdc/icon'
import { ToggleIconButton } from '@glare-labs/vue-mdc/icon-button'import { Icon } from '@glare-labs/vue-mdc/icon'
import { ToggleIconButton } from '@glare-labs/vue-mdc/icon-button'
import { Icon } from '@glare-labs/vue-mdc/icon'
import { ToggleIconButton } from '@glare-labs/vue-mdc/icon-button'
